# Abhängigkeiten
node_modules/

# Next.js
.next/
next-env.d.ts

# Testabdeckung
coverage/

# Betriebsdaten — Datenbank, Uploads, erzeugte PDFs, Sicherungen
#
# **Am Wurzelverzeichnis verankert** (führender Schrägstrich). Ohne ihn trifft
# ein Muster jedes gleichnamige Verzeichnis in jeder Tiefe — `storage/` schluckte
# so `src/infrastructure/storage/` mitsamt `artifact-store.ts` und
# `asset-store.ts`. Lokal lief alles, im CI fehlten die Module: Der Typcheck
# scheiterte an `Cannot find module '@/infrastructure/storage/artifact-store'`,
# und der Docker-Build gleich mit. Ein Fehler, den nur ein zweiter Rechner sieht.
/data/
/storage/
/backups/

# Konfiguration mit Geheimnissen (NFA-SEC-21)
.env
.env.local
.env.*.local

# Werkzeuge
*.tsbuildinfo
.DS_Store
dist/

# Fremde Dokumente in der Wurzel — nie Teil des Projekts
/*.pdf

# Wegwerfbestand des Aufnahmelaufs für die Bildschirmfotos (M16.1). Er entsteht
# in `npm run docs:shots` und wird dort auch wieder gelöscht; bleibt er nach
# einem Abbruch liegen, gehört er trotzdem nicht ins Repository.
/data/docs-shots.db*
/data/docs-shots-storage/
