apiVersion: apps/v1
kind: Deployment
metadata:
  name: faktura-app
  namespace: faktura
spec:
  replicas: 1

  selector:
    matchLabels:
      app: faktura-app

  template:
    metadata:
      labels:
        app: faktura-app

    spec:
      containers:
        - name: app
          image: ghcr.io/hiiirsch/faktura:1.0.2@sha256:316873afa45eb07a19ddc46775975af631ff3dda10b42c7fdb6791f9d5ed4f2e
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 3000
              protocol: TCP

          envFrom:
            - configMapRef:
                name: faktura-config

          volumeMounts:
            - name: data
              mountPath: /app/data

          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3

      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: faktura-data